Always use RTK for all shell commands in this Laravel project.

Rules:
- git → always use rtk git
- php artisan → always use rtk php artisan
- file reading → use rtk read
- directory listing → use rtk ls
- searching → use rtk grep

Never run raw commands unless explicitly requested.
Always prefer shell commands over internal tools.
